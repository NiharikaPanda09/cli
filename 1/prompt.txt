<cross-session-message from="uds:/tmp/cc-socks/8913.sock" from-name="implementer" from-mode="prompting">
Pushed two commits (75f72f854..f866f7c00): 636bcf81e "Show the nightly phase's installer report in the E2E log." (harness) and f866f7c00 "Assign the copy list before filtering it." (the fix). I did not wait for the dump: hypothesis (c) is about pwsh pipeline semantics, not Windows, so it was reproducible here — and it reproduced exactly.

## Diagnosis: (c), and it's an installer bug
Probe on pwsh 7.6.5 with a function returning two `{Source}` objects via `Write-Output -NoEnumerate`:
```
piped directly:  items seen by Where-Object = 1      <- the whole array as one object
assigned first:  items seen by Where-Object = 2
report line for the piped case: "! Also found:   A\entire.exe B\entire.exe"   <- both paths on ONE line
```
`$_.Source` on the array member-enumerates, so the report printed the stable and nightly copies space-joined on a single "Also found" line — `$report.Contains("! Also found:   <stableDir>\entire.exe")` then fails while the WARNING header still matches, exactly the CI shape. On 5.1, `-NoEnumerate` is not honoured through a pipeline, so the items enumerated and the report was right — hence green there. `Get-EntireOnPath` never hit this because every caller *assigned* it (`$pathCommands = Get-EntireOnPath`) before piping; my task-13 call site piped `Get-EntireCopy` straight into `Where-Object`. Not (a) or (b): the strings and the dedupe were fine — the elements were, and the count was wrong.

## Fix (install.ps1, +5/−2)
`$copies = Get-EntireCopy` then `$conflicting = @($copies | Where-Object …)`, with the comment now stating the rule: both lists are assigned before being piped because their -NoEnumerate output reaches a pipeline as one array on pwsh and as its items on 5.1. Confirmed the fixed shape prints one `! Also found:` line per copy on pwsh.

**The unit test had passed by the same accident** — `@(Get-EntireCopy | ForEach-Object { $_.Source })` member-enumerated `.Source` over the array and produced the right strings with the wrong element count. It now assigns, asserts `@($copies) | Should -HaveCount 2`, and indexes `$copies[0].Source` / `$copies[1].Source`; comment says why. That's the one that would have caught (c) before push, and now does.

## Harness (e2e.ps1, +2)
Phase 3 echoes `$report` with `Write-Host` right after capturing it, before any assertion, so the next failure — if any — shows the text it judged. Kept minimal rather than adding `-Context` plumbing to the assert helpers: the report is the only captured evidence in the script.

Verification here: suite 52 passed / 10 skipped; linter 0 on install.ps1 and e2e.ps1; fake-scoop clean; `mise run test:ps1` exit 0; guard still refuses; `-Help` fine. The pwsh E2E leg on f866f7c00 is the real check — and its log will now carry the report either way.
</cross-session-message>

---

<cross-session-message from="bridge:session_01PGkb9oEHJrrQYAmMvQAJKA" from-name="win-backend" from-mode="prompting">
f866f7c00 VERIFIED on real hardware — four `! Also found:` lines, and the child demonstrably could not have seen the fourth via its process PATH. Restore done. Not a regression.

## Step 6 re-run — FIXED ✅
Child spawned from a process whose env predates step 4's registry write, exactly the shape you asked for:
```
parent env contains entire-test-bin: False
--- spawning child ---
CHILD PID: 2860
child env contains entire-test-bin: False
child env[0]: C:\Users\Victor\tools2
binary really exists there: True
```
Your requested precondition holds: `entire-test-bin` is **absent** from the child's `$env:Path`, while `entire-test-bin\entire.exe` really is on disk. Then:
```
==> Installing Entire CLI...
Warning: Scoop only publishes stable releases; installing nightly from the verified release archive.
==> Detected platform: windows/arm64
==> Fetching latest nightly version...
==> Installing version: 0.10.6-nightly.202609040625.7c63555a3
==> Downloading entire_windows_arm64.zip...
==> Downloading checksums...
==> Verifying checksum...
==> Extracting...
==> Installing to C:\Users\Victor\entire-test-nightly...
==> Entire CLI installed to C:\Users\Victor\entire-test-nightly\entire.exe

! WARNING: PATH conflict detected
!
! Installed to: C:\Users\Victor\entire-test-nightly\entire.exe
! Also found:   C:\Users\Victor\tools2\entire.exe
! Also found:   C:\Users\Victor\tools\entire.exe
! Also found:   C:\Users\Victor\scoop\shims\entire.exe
! Also found:   C:\Users\Victor\entire-test-bin\entire.exe
!
! C:\Users\Victor\tools2\entire.exe is on the machine-wide PATH, which Windows places
! ahead of your user PATH, so it wins in a new terminal however your
! user PATH is ordered. Remove that copy, or drop its directory from
! the machine PATH (needs an administrator). To install over it
! instead, rerun with:
!   -InstallDir C:\Users\Victor\tools2

==> Added C:\Users\Victor\entire-test-nightly to your user PATH
Restart your terminal, then run entire to get started.
NO THROW
```
Four lines, the fourth being the copy the previous run installed. The stored-PATH union is doing exactly what it was written to do, and the negative control makes it airtight — the child had no way to reach that path through `$env:Path`.

**One observation on ordering, not a defect but worth a decision.** `entire-test-bin` lands **last**, after the Scoop shim, rather than in user-PATH order — where it sits *first*. So the list is "process-PATH entries in resolution order, then stored-PATH-only additions". That reads fine as a set, but the lines look like a priority ranking, and here the last line is the entry that is actually second in the stored user PATH. If someone scans the list top-down to decide what to delete first, the ordering is misleading. Sorting the union by stored-PATH position, or visually separating the two groups ("also on your stored PATH, not active in this shell:"), would remove the ambiguity. Your call — it did not affect correctness of the report.

Step 4 at f866f7c00 was byte-for-byte the same behaviour as at 322b4bf18: three Also-found lines (correct — `entire-test-nightly` did not exist yet), machine-PATH paragraph, REG_SZ preserved, `entry[0] = entire-test-bin`, no throw. The pwsh `-NoEnumerate` fix is untestable here as you said (5.1 only, no pwsh).

## Registry assertions
```
entry[0]: C:\Users\Victor\entire-test-nightly
entry[1]: C:\Users\Victor\entire-test-bin
rest==snap: True
Entire CLI 0.10.6-nightly.202609040625.7c63555a3 / go1.26.6 / windows/arm64
```
Ordering, byte-identical remainder, REG_SZ, and a working arm64 nightly — all as before.

## Step 9 — restored
```
PATH restored exactly: True
kind: String
broadcast sent
test dirs gone: True
.local\bin intact: True
C:\Users\Victor\tools2\entire.exe
C:\Users\Victor\tools\entire.exe
C:\Users\Victor\scoop\shims\entire.exe
```
With `[Enum]::Parse` and the closing broadcast, as before. Machine is clean: PATH byte-identical, REG_SZ, resolution order back to tools2 → tools → shim, Scoop untouched, `.local\bin` untouched. Pester/PSScriptAnalyzer/NuGet provider remain from the earlier run, as agreed.

## One thing for whoever picks up the test work
I've suggested to my user that the two-run-two-process sequence become a real `e2e.ps1` phase — install to dir A in one child, dir B in the next, assert B's report names A. That is precisely this test, it needs no network beyond what phase 1 already does, and right now the only evidence f866f7c00 works is a human on one ARM64 laptop. Same for the machine-PATH paragraph (plant a stub `entire.exe`, append its dir to the machine PATH — the runner is admin) and a `Get-PlatformArchitecture` unit test, which currently has **no coverage at all**. If task 13 is still open, those three are cheap and would have caught this finding without me.

Nothing else outstanding here.
</cross-session-message>